# 投递材料 · 清程极智 智能计算研发工程师

> 投递方式：飞书直投 `chitu-ai.jobs.feishu.cn`（岗位 ID 7621017904042314003）
> 岗位：智能计算研发工程师 | 清程极智（chitu-ai）
> 学历：**本科 + 3 年+ 不卡学历** — JD 原文要求本科及以上。

---

## 核实结论

1. **投递入口（飞书招聘）**：飞书招聘页 `chitu-ai.jobs.feishu.cn` 搜索岗位 ID `7621017904042314003` 或"智能计算"→ 进入岗位详情 → 点"投递简历"→ 上传 PDF + 填基本信息 → 提交。飞书招聘支持手机号验证码或微信扫码登录。
2. **JD 关键词（据第 53 轮任务说明）**：跨芯片训推加速软件栈 / 算子库 / 模型并行 / 显存管理；优先项：**RDMA/NVLink 通信调优 / HPC / Slurm**。本科 + 3 年+ 不卡学历。
3. **匹配度评估**：候选人百度云 IaaS RDMA/GPU 集群 + 上交所 UDP 低延时 + eBPF 系统级性能分析 + CUDA/Triton 可迁移——对口"通信调优/HPC/系统性能"。算子库/模型并行侧无直接生产经验，靠系统底层 + 通信调优迁移。
4. **HR / 内推**：飞书招聘无独立 HR 邮箱。内推渠道：①脉脉搜"清程极智 内推"；②官网 chitu-ai.com 招聘页；③飞书"分享岗位"获取内推链接。建议先脉脉内推 + 飞书直投双管齐下。

---

## ✉️ 飞书投递"自我介绍/求职信"框（可粘贴）

您好，我是王毅博，应聘「智能计算研发工程师」。7 年 Linux 内核 + 大规模系统性能调优，与 JD 的跨芯片训推加速软件栈、RDMA/NVLink 通信调优、HPC 方向高度契合：

**① RDMA/NVLink 通信调优（JD 优先项，核心）**
- 上交所 UDP 网关：**15 万并发平均延时 <100μs**——软中断绑核 + 中断聚合 + 网络栈调优 + **gazelle/DPDK 用户态零拷贝**。生产级"低时延、高带宽通信"硬证据，直接对口训推集群的通信调优。
- 统信 4 年服务器内核网络协议栈全模块维护；定位过 ARM 二层转发软中断占满 CPU（ebt_do_table 读写锁 + LSE 原子锁）等通信性能疑难。

**② 大规模 IaaS / GPU 集群基础设施（百度云，对口 HPC/Slurm）**
- 现职百度云 IaaS 镜像/软件源研发（2025.7 起）：~100TB 跨区域存储一致性架构重构，分布式 IO/网络调优——对口 HPC 集群存储/调度基础设施。
- 大规模集群稳定性视角：跨芯片/多机型适配、镜像分发、容量基线测算——可直接迁移到跨芯片训推加速软件栈的多芯片适配。

**③ 系统级性能分析（JD 算子库/显存管理调优加分）**
- eBPF(bcc/libbpf) + perf/ftrace/Crash 长期实战；向 Linux 内核 **BPF 子系统合入约 1000 补丁**。
- 算子库/显存管理的性能 profile（缓存/分支预测/锁竞争/内存带宽）可由内核级性能分析能力迁移——eBPF/perf 是最强可观测武器，可用于 GPU/通信链路瓶颈根因分析。

**④ CUDA/Triton 可迁移**
- CUDA 无直接生产经验，但 GPU 编程模型理解到位（百度云 GPU 集群 IaaS 侧接触）；系统底层 + 通信调优迁移成本低。

**⑤ C/C++ 基线**
- C 精通（7 年内核 C），Shell/Python 熟练，C++ 基础。

JD 优先项 RDMA/NVLink 通信调优 + HPC 是我的核心强项（低延时网络 + 大规模 IaaS），算子库/模型并行侧靠系统性能分析能力迁移。期待进一步沟通。

---

## 📄 简历需突出的要点（基于 resume-大模型Infra版.md 调整）

首页一句话定位（置顶）：
> **7年Linux内核 | BPF约1000补丁 | 高性能低延时通信(15万并发<100μs/DPDK) | 百度云IaaS RDMA/GPU集群 | eBPF系统级性能分析**

1. **上交所 UDP <100μs 置顶**：作为"RDMA/NVLink 通信调优"硬证据，写明 gazelle/DPDK 用户态零拷贝 + 软中断绑核 + 中断聚合——命中 JD 优先项。
2. **百度云 IaaS 强化**：~100T 跨区域存储一致性 + 多机型适配——对口"HPC/Slurm/跨芯片训推加速软件栈"。
3. **eBPF/perf 系统级性能分析**：BPF ~1000 补丁 + bcc/libbpf，包装为"算子库/显存管理/通信链路性能 profile 与根因分析"能力。附 LKML 链接。
4. **网络栈深度**：统信 4 年服务器内核网络协议栈维护 + 二层转发锁竞争疑难——展示通信底层功底。
5. **GPU/CUDA 诚实标注**：无直接 CUDA 生产经验，但百度云 GPU 集群 IaaS 侧接触 + 系统底层迁移成本低，面试坦承。
6. **弱化学历**：本岗本科 + 3 年+ 不卡学历，教育放靠后；用通信调优 + IaaS + eBPF 盖过。
7. **C/C++ 基线**：满足任职资格。

---

## ⏱️ 投递操作步骤（约 5 分钟）

1. 打开 `https://chitu-ai.jobs.feishu.cn`（或岗位直达链接含 ID 7621017904042314003）
2. 搜索"智能计算"或直接进入岗位 ID 7621017904042314003 详情页
3. 点"投递简历"→ 手机号验证码 / 微信扫码登录
4. 上传**中文简历 PDF**（命名 `王毅博-简历-智能计算研发工程师.pdf`，基于 resume-大模型Infra版.md 导出）
5. 将上方【自我介绍】粘贴到求职信/自我介绍框（如有）→ 填基本信息（教育填本科西安理工大学，工作经历按简历顺序）→ 提交
6. **脉脉内推**：搜"清程极智 内推"找员工，发岗位标题 + 简历，请其点"分享岗位"生成内推链接
7. 同公司可顺投"分布式 / 通信 / 性能"相关岗作第二选择

> ⚠️ 本岗本科 + 3 年+ 不卡学历，RDMA/NVLink 通信调优优先项是候选人强项，匹配度高，建议本周内投。
> ⚠️ 简历用 resume-大模型Infra版.md，重点强化"通信调优 + IaaS GPU 集群"两段。
> ⚠️ 算子库/模型并行侧无直接经验，靠系统性能分析能力迁移，面试话术要准备好。
