# 投递材料 · 星动纪元 RobotEra Linux系统软件及BSP驱动工程师

> 投递方式：飞书直投 `k0fqxcszc9.jobs.feishu.cn`（岗位 ID 7571785644297521459）
> 岗位：Linux 系统软件及 BSP 驱动工程师 | 星动纪元 RobotEra
> 学历：**本科可投** — JD 要求 3+ 年嵌入式 Linux 内核/BSP/驱动经验，未卡 211/985/硕士。

---

## 核实结论

1. **投递入口（飞书招聘）**：飞书招聘页 `k0fqxcszc9.jobs.feishu.cn` 搜索岗位 ID `7571785644297521459` 或"Linux 系统 BSP"→ 进入岗位详情 → 点"投递简历"→ 上传 PDF + 填基本信息（教育/工作经历）→ 提交。飞书招聘支持手机号验证码或微信扫码登录。
2. **JD 关键词（据第 53 轮任务说明）**：机器人算力单元 Linux 裁剪/优化；SoC BSP（U-Boot/设备树/内核配置/驱动适配）；外设驱动（Ethernet/EtherCAT/PCIe/USB/SPI/I2C/CAN）；系统性能优化（调度/中断/实时性/PTP/资源隔离）；量产（OTA/故障诊断/安全）；3+ 年嵌入式 Linux 内核/BSP/驱动 C/C++/Shell；熟悉 NVIDIA/Horizon/Rockchip/Qualcomm SoC。
3. **匹配度 95+（量身定制）**：闻翔 Zynq 全栈 bring-up（U-Boot/设备树/驱动/rootfs）直接命中"SoC BSP + 外设驱动"；上交所 UDP 15 万并发 <100μs 命中"系统性能优化/实时性/中断优化"；统信 4 年内核命中"内核配置/调度/中断"。本科可投不卡学历，是本轮最高匹配岗之一。
4. **HR / 内推**：飞书招聘无独立 HR 邮箱。内推渠道：①脉脉搜"星动纪元 内推"找员工；②官网 robotera.com 招聘页；③飞书招聘页"分享岗位"获取内推链接。建议先脉脉内推 + 飞书直投双管齐下。

---

## ✉️ 飞书投递"自我介绍/求职信"框（可粘贴）

您好，我是王毅博，应聘「Linux 系统软件及 BSP 驱动工程师」。7 年 Linux 内核与嵌入式系统研发，板级 bring-up 全栈 + 内核态实时性调优 + 量产稳定性可定位性的组合，与岗位 JD 几乎逐条命中：

**① SoC BSP / U-Boot / 设备树 / 驱动适配（JD 核心职责）**
- 闻翔 Zynq（ARM SoC + FPGA）平台**全栈 bring-up**：独立完成 U-Boot 移植 / Linux 内核适配 / rootfs 构建 / **设备树编写**（SPI/LCD/触摸屏/PL UartLite）/ 板级驱动移植，从上电到应用全链路调通。
- AD9361 射频驱动开发 + 约 7000 行 FPGA 控制代码重构——具备硬件接口调试能力（示波器/频谱仪），不仅是内核裁剪，更能定位板级时序与接口问题。

**② 外设驱动（Ethernet/PCIe/USB/SPI/I2C/CAN）**
- 闻翔期间 SPI/LCD/触摸屏/UartLite 设备树与驱动全独立完成；统信期间维护服务器内核网络协议栈、PCIe 设备驱动兼容性、USB 子系统疑难。
- 上交所项目独立编写**网卡中断信息展示内核模块约 300 行**——直接展示"写驱动 + 调中断"能力。

**③ 系统性能优化 / 实时性 / 调度 / 中断（JD 强调项）**
- 上交所 UDP 网关：**15 万并发平均延时 <100μs**——软中断绑核 + 中断聚合 + 网络栈参数调优 + 用户态零拷贝（gazelle/DPDK）。这是机器人控制周期低延时 + 通信栈实时性的硬证据。
- 统信 4 年内核调度/中断/内存子系统调优，定位过 ARM 二层转发软中断占满 CPU（ebt_do_table 读写锁 + LSE 原子锁）等实时性疑难。

**④ 资源隔离 / PTP / 量产稳定性**
- 内核 cgroup/namespace 长期使用，对口算力单元资源隔离；网络栈时间戳/PTP 相关调试经验。
- ftrace/perf/eBPF/Crash 定位疑难宕机（硬件单 bit 翻转 → do_page_fault）；kpatch 热补丁产品化（仿红帽 rpm spec 打包）——直接对应"量产故障诊断/安全/OTA 修复"。

**⑤ SoC 适配能力（JD 优先项）**
- Zynq ARM + 申威 + 飞腾 + x86 多架构内核适配经验，对 SoC BSP 的迁移成本理解到位；虽未直接做过 NVIDIA Jetson/Horizon J5，但 ARM SoC bring-up 流程通用，可快速迁移。

**⑥ C/C++/Shell 基线**
- C 精通（7 年内核 C），Shell/Python 熟练，C++ 基础。

JD 几乎每一条都能挂上一段战绩，期待与团队进一步沟通机器人算力单元的 BSP 与实时性挑战。

---

## 📄 简历需突出的要点（基于 resume-机器人版.md 调整）

首页一句话定位（置顶）：
> **7年Linux内核/BSP/驱动 | Zynq全栈bring-up(U-Boot/设备树/驱动/rootfs) | 硬实时UDP 15万并发<100μs | BPF约1000补丁 | 量产稳定性(kpatch/ftrace/Crash)**

1. **闻翔 Zynq 全栈 bring-up 置顶**：U-Boot 移植 / 内核适配 / rootfs / 设备树（SPI/LCD/触摸屏/UartLite）/ AD9361 驱动 / ~7000 行 FPGA 重构——这是命中"SoC BSP + 外设驱动"的最强项，写明"独立完成从 boot 到应用的整板可用系统"。
2. **上交所 UDP <100μs**：作为"系统性能优化 / 实时性 / 中断优化"硬证据，写明软中断绑核 + 中断聚合 + 网络栈调优 + gazelle/DPDK 用户态零拷贝；并附"独立编写网卡中断信息展示内核模块 ~300 行"。
3. **外设驱动清单显式列出**：SPI / LCD / 触摸屏 / UartLite / Ethernet / PCIe / USB——直接命中 JD 关键词。
4. **量产稳定性能力**：ftrace/perf/eBPF/Crash 疑难定位 + kpatch 热补丁产品化——对应"OTA/故障诊断/安全"。
5. **多 SoC 适配**：Zynq ARM / 申威 / 飞腾 / x86——展示 SoC 迁移能力，回应"NVIDIA/Horizon/Rockchip/Qualcomm"优先项。
6. **BPF ~1000 补丁**：内核深改能力背书，附 LKML 链接。
7. **弱化学历**：本岗本科可投，教育放靠后；Zynq 实战 + 内核补丁是盖过学历的硬通货。
8. **诚实标注**：未直接做过 NVIDIA Jetson/Horizon J5 SoC——简历不必编造，面试坦承"ARM SoC bring-up 流程通用，Zynq 经验可快速迁移"。

---

## ⏱️ 投递操作步骤（约 5 分钟）

1. 打开 `https://k0fqxcszc9.jobs.feishu.cn`（或岗位直达链接含 ID 7571785644297521459）
2. 搜索"Linux 系统 BSP"或直接进入岗位 ID 7571785644297521459 详情页
3. 点"投递简历"→ 手机号验证码 / 微信扫码登录
4. 上传**中文简历 PDF**（命名 `王毅博-简历-Linux系统软件及BSP驱动工程师.pdf`，基于 resume-机器人版.md 导出）
5. 将上方【自我介绍】粘贴到求职信/自我介绍框（如有）→ 填基本信息（教育填本科西安理工大学，工作经历按简历顺序）→ 提交
6. **脉脉内推**：搜"星动纪元 内推"找员工，发岗位标题 + 简历，请其点"分享岗位"生成内推链接（可绕初筛）
7. 同公司可顺投"嵌入式 / 实时性 / 系统稳定性"相关岗作第二选择

> ⚠️ JD 95+ 量身定制，闻翔 Zynq 全栈 bring-up 直接命中核心职责，是本轮最高优岗之一，建议本周内投递。
> ⚠️ 简历用 resume-机器人版.md，重点强化"外设驱动清单 + 量产稳定性"两段。
