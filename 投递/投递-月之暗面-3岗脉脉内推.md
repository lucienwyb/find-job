# 投递材料 · 月之暗面 Moonshot 3岗（脉脉内推优先 + mokahr 直投）

> 投递方式：**必须先脉脉内推**（6 个月仅 3 岗），内推后再走 mokahr 在线申请。
> 简历：使用 `resume-大模型Infra版.md`（导出 PDF）。
> 投递顺序：① 高性能分布式存储工程师（07-16 发，eBPF 命中 90+）→ ② Coding Agent 研发工程师 Kimi Code（06-30，匹配 88）→ ③ Infra 系统应用工程师（12-02，Agent 方向 82）。

---

## 核实结论

1. **3 岗信息（据第 53 轮任务说明）**：
   - ① **高性能分布式存储工程师**（07-16 发布，命中 90+）：Perf/eBPF + IOUring/NVME/SPDK/RDMA + C++/Rust/Go。
   - ② **Coding Agent 研发工程师 Kimi Code**（06-30，匹配 88）：内核调度 / 系统底层 C++ / JNI / Cursor / Claude。
   - ③ **Infra 系统应用工程师**（12-02，Agent 方向 82）。
2. **脉脉内推是首选路径**：Moonshot 走 mokahr 门户，但脉脉内推可绕初筛、加 visibility，且 6 个月仅 3 岗的限额下应优先把内推名额给最高匹配岗（① 高性能分布式存储）。
3. **mokahr 门户**：Moonshot mokahr slug 待脉脉内推人提供（或搜 `moonshot` / `kimichat`）。流程与寒武纪/银河通用一致：登录 → 搜索岗位 → 申请 → 上传 PDF + 填表。
4. **匹配度**：岗① eBPF/Perf 命中 90+（BPF ~1000 补丁 + perf 实战 + 百度云 ~100T 分布式分发）；岗② 88（内核调度 + 系统底层 C/C++ + eBPF 可观测，但 Cursor/Claude Agent 侧无直接经验，靠系统底层迁移）；岗③ 82（Agent 方向）。
5. **学历**：Moonshot 大模型 Infra 类岗普遍不卡 211（看实战），但部分岗偏好硕士——岗① 偏存储/系统性能，本科 + 7 年内核实战有机会；岗②③ Agent 方向竞争激烈，需强战绩盖过。

---

## ✉️ 脉脉内推私信话术（3 段，按岗替换）

### 岗① 高性能分布式存储工程师（首选，先发）

> 你好，我是王毅博，7 年 Linux 内核研发，想内推 Moonshot「高性能分布式存储工程师」（07-16 发布）。
>
> 核心匹配点：
> - **eBPF/Perf 命中 90+**：向 Linux 内核 BPF 子系统合入约 1000 补丁；长期 perf/ftrace/eBBF 实战。
> - **IOUring/NVMe/SPDK/RDMA 相关**：统信 4 年服务器内核，定位过申威 io_uring fork io 线程段错误；上交所项目 gazelle/DPDK 用户态零拷贝 + 15 万并发 UDP <100μs；现职百度云 IaaS ~100T 软件源跨区域一致性架构重构（pulp 分析）。
> - C/C++/Shell/Go 基线满足，7 年系统底层经验。
>
> 简历附上，能否帮忙内推？感谢！

### 岗② Coding Agent 研发工程师 Kimi Code（第二发）

> 你好，我是王毅博，7 年 Linux 内核 + 系统底层 C/C++ 研发，想内推 Moonshot「Coding Agent 研发工程师 Kimi Code」。
>
> 匹配点：
> - **内核调度 / 系统底层 C/C++**：4 年统信服务器内核，调度/内存/中断子系统全模块开发；C 精通，长期内核态 + 用户态系统级编程。
> - **可观测 / Agent 工具链可迁移**：eBPF/bcc/libbpf + ftrace/perf/Crash 定位疑难；对 LLM Coding Agent 的执行环境、沙箱、性能 profile 有系统底层视角。
> - **Cursor/Claude 侧**：日常重度使用 Cursor + Claude Code 做 kernel/系统开发，理解 Coding Agent 工作流。
>
> 简历附上，能否帮忙内推？感谢！

### 岗③ Infra 系统应用工程师（第三发）

> 你好，我是王毅博，7 年 Linux 内核 + 大规模系统 Infra 经验，想内推 Moonshot「Infra 系统应用工程师」（Agent 方向）。
>
> 匹配点：
> - 内核态稳定性 + 集群可观测：BPF ~1000 补丁 + ftrace/perf/eBPF/Crash 疑难定位（panic/内存泄漏/性能瓶颈）。
> - 百度云 IaaS ~100T 软件源跨区域一致性架构重构——大模型 Infra 镜像/权重多区域分发的直接对口经验。
> - 上交所 UDP 15 万并发 <100μs，低延时集群通信调优。
>
> 简历附上，能否帮忙内推？感谢！

---

## ✉️ mokahr 申请填表要点 + 自我介绍（粘贴到求职信框）

### 岗① 高性能分布式存储工程师（自我介绍）

您好，我是王毅博，应聘「高性能分布式存储工程师」。7 年 Linux 内核 + 大规模系统性能调优，与 JD 的 Perf/eBPF + IOUring/NVMe/SPDK/RDMA + C++/Rust/Go 高度契合：

**① Perf/eBPF（JD 核心，命中 90+）**
- 向 Linux 内核 **BPF 子系统合入约 1000 补丁**（verifier/bpf syscall/BTF）；长期 bcc/libbpf 实战。
- perf/ftrace/火焰图日常定位存储/IO/网络性能瓶颈。

**② IOUring / NVMe / SPDK / RDMA 相关**
- 统信 4 年服务器内核，定位过**申威 io_uring fork io 线程段错误**（架构代码级疑难）——直接对口 io_uring 稳定性。
- 上交所项目 **gazelle/DPDK 用户态零拷贝** + 15 万并发 UDP <100μs；内核网络栈深改（bpf 回合 ~1000 补丁）——RDMA/SPDK 用户态高性能 IO 路径可迁移。
- 百度云 IaaS：~100T 软件源跨区域存储一致性架构重构（开源 pulp 分析，单容器 QPS/带宽测算）——直接对口分布式存储。

**③ C/C++/Rust/Go 基线**
- C 精通（7 年内核 C），Shell/Python 熟练，C++/Go 基础；Rust 无生产经验但系统底层迁移成本低。

期待进一步沟通。

### 岗② Coding Agent 研发工程师 Kimi Code（自我介绍）

您好，我是王毅博，应聘「Coding Agent 研发工程师 Kimi Code」。7 年 Linux 内核 + 系统底层 C/C++，与 JD 的内核调度/系统底层 + Agent 工具链方向契合：

**① 内核调度 / 系统底层 C/C++（JD 核心）**
- 统信 4 年服务器内核：调度/内存（伙伴/SLAB）/VFS/网络栈/中断/锁全模块开发与调优。
- C 精通，内核态 + 用户态系统级编程；JNI 侧无直接经验但有跨语言 FFI 理解。
- 上交所 UDP 15 万并发 <100μs：调度/中断/锁竞争调优实战。

**② 可观测 / Agent 执行环境**
- eBPF/bcc/libbpf + ftrace/perf/Crash 定位疑难——可迁移到 Coding Agent 沙箱执行环境的性能 profile 与故障定位。
- 重度使用 Cursor + Claude Code 做 kernel/系统开发，理解 Coding Agent 工作流与痛点。

期待进一步沟通。

### 岗③ Infra 系统应用工程师（自我介绍）

您好，我是王毅博，应聘「Infra 系统应用工程师」（Agent 方向）。7 年 Linux 内核 + 大规模 Infra 经验：

- 内核态稳定性：BPF ~1000 补丁 + ftrace/perf/eBPF/Crash 疑难定位（panic/内存泄漏/性能瓶颈）。
- 集群 Infra：百度云 IaaS ~100T 软件源跨区域一致性架构重构——大模型 Infra 镜像/权重多区域分发直接对口。
- 低延时通信：上交所 UDP 15 万并发 <100μs。
- kpatch 热补丁产品化：集群滚动升级/灰度回滚经验。

期待进一步沟通。

---

## 📄 简历需突出的要点（resume-大模型Infra版.md）

1. **三条硬数字置顶**：15 万并发 UDP <100μs / BPF ~1000 补丁 / ~100T 软件源跨区域一致性——分别覆盖岗①②③的硬通货。
2. **岗① 强化**：申威 io_uring fork io 线程段错误（io_uring 稳定性硬证据）+ gazelle/DPDK 用户态零拷贝 + 百度云 ~100T 分布式存储——把"IOUring/NVMe/SPDK/RDMA"关键词显式挂上。
3. **岗② 强化**：内核调度/中断/锁竞争调优 + Cursor/Claude Code 日常使用——Agent 方向可迁移性。
4. **岗③ 强化**：百度云 IaaS 多区域分发 + kpatch 灰度回滚——大模型 Infra 集群运维对口。
5. **BPF ~1000 补丁附 LKML 链接**：所有 3 岗通用强背书。
6. **弱化学历**：教育放靠后；用 BPF 补丁 + 百度云 IaaS + 上交所实战盖过。
7. **诚实标注**：Rust 无生产经验（岗①）/ JNI 无直接经验（岗②）——简历不必编造，面试坦承"系统底层迁移成本低"。

---

## ⏱️ 投递操作步骤

### A. 脉脉内推（优先，3 岗各找 1 个内推人）

1. 脉脉搜"Moonshot 内推" / "月之暗面 内推" / "Kimi 内推"——找在职员工
2. 按上方 3 段话术分别私信（**先发岗① 高性能分布式存储，匹配最高**）
3. 附 `resume-大模型Infra版.md` 导出的 PDF（命名 `王毅博-简历-大模型Infra工程师.pdf`）
4. 内推人提供 mokahr 内推链接 → 走内推通道投递

### B. mokahr 直投（若无内推或内推后补投）

1. 打开 Moonshot mokahr 门户（脉脉内推人提供 slug，或搜 `app.mokahr.com/apply/moonshot`）
2. 按 ① → ② → ③ 顺序搜索岗位名 → 点"申请职位"
3. 上传**中文简历 PDF** → 填基本信息/教育(本科)/工作经历 → 将上方对应岗位【自我介绍】粘贴到求职信框 → 提交
4. 每岗投递后截图保存申请号

### C. 注意事项

> ⚠️ 6 个月仅 3 岗限额：必须把内推名额优先给岗① 高性能分布式存储（匹配 90+），岗②③ 视内推人响应情况再投。
> ⚠️ 简历统一用 resume-大模型Infra版.md，但岗①②投递时可微调置顶定位句（岗①强调"eBPF/IOUring/分布式存储"，岗②强调"内核调度/系统底层 C/C++"）。
> ⚠️ Moonshot 流程偏快，脉脉内推响应通常 1-3 天，建议本周内完成岗①内推。
