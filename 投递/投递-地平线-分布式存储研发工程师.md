# 投递材料 · 地平线 分布式存储研发工程师

> 投递方式：hotjob 门户在线申请 `wecruit.hotjob.cn/SU64819a4f2f9d2433ba8b043a`
> 岗位：分布式存储研发工程师 | AI基础架构部 | 北京
> 学历：**本科可投**（JD 未要求 211/985、未要求硕士；优先项直接点名"内核/操作系统开发经验者优先"）
> 新发：2026-07-15（第54轮AA Playwright 渲染确认在招）
> 本轮优先级：★★★ 第55轮第一优先

---

## 核实结论（第55轮）

1. **岗位确认在线（第54轮AA已核实）**：第54轮AA 通过 Playwright 渲染 hotjob 门户确认「分布式存储研发工程师」于 **2026-07-15 新发**，部门 **AI基础架构部**，工作地北京。JD 优先条件**直接点名"内核、操作系统相关开发经验者优先"+"Linux I/O栈优先"**，候选人7年内核/eBPF 完美命中。同部门 r44 抓取数据（2026-06-30）另有"存储&湖仓高级研发工程师"(postId `6a432e6487f0b47896f5e72f`)，与本岗（7-15新发"分布式存储研发工程师"）是两个不同岗位。
2. **jobId/直链（本轮未能直接取得，原因见下）**：hotjob 门户对该数据中心代理 IP 返回 **WAF 拦截页**——多次重试均返回 `Sorry, your request has been blocked as it may cause potential threats to the server's security`（2026-07-16 实测）。因此自动化无法取到本岗 postId。**候选人须用本人浏览器（家庭宽带，非代理）打开门户搜索**。r44 数据确认 hotjob 职位直链格式为 `wecruit.hotjob.cn/SU64819a4f2f9d2433ba8b043a/pb/social.html`，进入后搜索"分布式存储"即可看到岗位详情与"申请"按钮。
3. **投递流程（hotjob 在线申请）**：① 打开 `https://wecruit.hotjob.cn/SU64819a4f2f9d2433ba8b043a/pb/social.html`（**必须用本人浏览器，代理IP已被WAF封**）→ ② 搜索"分布式存储研发工程师" → ③ 进入岗位详情 → ④ 点"申请职位"→ 注册/登录（手机号）→ 填基本信息/教育/工作经历 → 上传**中文简历 PDF** → 提交。
4. **是否卡学历**：JD 原文任职要求为本科及以上（未注明院校层次）；优先项是"内核/OS经验"而非学历。**本科可投，不卡211**。西安理工大学本科完全符合硬门槛，用7年内核+~1000 BPF补丁盖过学历。
5. **JD 要点（第54轮AA实抓）**：
   - 技术栈：JuiceFS / Ceph / MinIO + Linux C/C++ / Go。
   - **优先条件（直接点名）**：①"内核、操作系统相关开发经验者优先"；②"Linux I/O 栈优先"。
   - 方向：AI 训练/智驾数据场景下的分布式存储基础设施（结合地平线智驾+AI训练数据规模）。
6. **匹配度 ★★★★★**：候选人"统信4年内核（FS/IO栈/块层）+ bpf~1000补丁 + 百度云IaaS（~100TB跨区存储一致性）+ 上交所UDP高性能"几乎逐条命中"Linux I/O栈/内核/分布式存储"。

---

## ✉️ 申请填表要点 + 正文（粘贴到"自我介绍/求职信"框）

**申请职位**：分布式存储研发工程师（AI基础架构部，北京）

**自我介绍 / 求职信（粘贴）：**

您好，我是王毅博，应聘「分布式存储研发工程师」。我的背景在 Linux 内核 I/O 栈、文件系统与大规模存储基础设施上与岗位优先条件高度契合——JD 直接点名"内核/操作系统开发经验者优先""Linux I/O栈优先"，以下逐条挂战绩：

**① Linux 内核 I/O 栈 / 文件系统（JD 优先项核心，直接命中）**
- 统信 4 年 UOS 服务器内核维护，覆盖 **VFS / 块层 / 文件系统（ext4/xfs/btrfs）/ IO 调度 / page cache / 直接 IO** 全栈——这就是"Linux I/O 栈"。
- 向 Linux 内核 **BPF 子系统合入约 1000 个补丁**（verifier/syscall/BTF/程序类型扩展）——内核深改能力的硬背书。附 LKML commit 链接。

**② 分布式存储 / 大规模存储基础设施（JD JuiceFS/Ceph/MinIO 对口）**
- 现职百度云 IaaS 镜像/软件源研发（2025.7 起）：**~100TB 跨区域存储一致性**架构、大规模分发与分布式 IO 调优——分布式存储/对象存储的生产实战，直接对口"分布式存储基础设施"。
- 对对象存储/块/文件三类接口的工程权衡有生产级理解（IaaS 软件源本质是大规模多区域存储+分发）。

**③ C/C++/Go 基线 + 性能调优**
- C 精通（7 年内核 C），C++/Go 可上手（JD 要求 Linux C/C++/Go）。
- 上交所 UDP 网关 **15 万并发平均延时 <100μs**：软中断绑核 + 中断聚合 + 锁竞争调优——大规模系统级性能调优实战，可迁移到存储 IO 性能瓶颈分析。
- ftrace/perf/eBPF(bcc/libbpf)/Crash 定位疑难（io_uring 段错误、硬件单 bit 翻转→do_page_fault）——存储 IO 链路 profile 与故障根因分析的最强可观测武器。

**④ 内核/操作系统开发经验（JD 优先项直接命中）**
- 7 年 Linux 内核研发，调度/内存/VFS/网络栈/中断/锁/namespace/cgroup 全模块开发与调优；kpatch 热补丁产品化。

JD 优先项"内核/OS经验 + Linux I/O栈"是我最对口的强项。期待进一步沟通分布式存储的系统挑战。

---

## 📄 简历需突出的要点（基于 resume-大模型Infra版.md 调整）

首页一句话定位（置顶）：
> **7年Linux内核 | BPF约1000补丁(附LKML链接) | Linux I/O栈/FS深改(统信4年) | 百度云IaaS ~100TB分布式存储 | 上交所UDP 15万并发<100μs**

1. **Linux I/O 栈置顶**（JD 优先项直接点名）：统信4年 VFS/块层/文件系统/IO调度/page cache/直接IO 全栈——这是命中"Linux I/O栈优先"的最强项，单独成段。
2. **bpf~1000补丁挂 LKML 链接**：内核深改硬背书，所有内核岗通用。
3. **百度云 IaaS ~100TB 跨区存储一致性**：对口 JuiceFS/Ceph/MinIO 分布式存储——把"软件源/镜像"重新包装为"大规模分布式存储+多区域分发基础设施"。
4. **上交所 UDP 15万并发<100μs**：大规模系统级性能调优，迁移到存储 IO 性能瓶颈分析。
5. **eBPF/perf/Crash 可观测**：存储 IO 链路 profile + 故障根因分析。
6. **C 精通 + C++/Go**：满足 JD 语言基线（C++/Go 可标注"可上手/基础"）。
7. **弱化学历**：本科可投、不卡211，教育放靠后；内核I/O栈+bpf补丁+百度云IaaS是盖过学历的硬通货。
8. **诚实标注**：JuiceFS/Ceph/MinIO 若无直接生产使用经验，简历不必编造——用"内核I/O栈深改 + 百度云IaaS大规模分布式存储"作为对口证据，面试坦承"理解分布式存储架构，内核IO栈+大规模存储运维可快速迁移到 JuiceFS/Ceph 深度开发"。

---

## ⏱️ 投递操作步骤（约 5 分钟）

1. **用本人浏览器（家庭宽带，非数据中心代理）**打开 `https://wecruit.hotjob.cn/SU64819a4f2f9d2433ba8b043a/pb/social.html`
   - ⚠️ 第55轮核实：hotjob 已对数据中心代理 IP 启用 WAF 拦截（"request has been blocked as it may cause potential threats"），代理/自动化无法访问。**必须候选人本人浏览器打开**。
2. 搜索"分布式存储研发工程师"→ 确认发布日期 2026-07-15、部门 AI基础架构部、地点北京
3. 进入岗位详情 → 核对 JD 优先项是否仍含"内核/OS经验者优先""Linux I/O栈优先"
4. 点"申请职位"→ 注册/登录（手机号验证码）→ 填基本信息/教育(本科·西安理工大学)/工作经历 → 上传**中文简历 PDF**（命名 `王毅博-简历-分布式存储研发工程师.pdf`，基于 resume-大模型Infra版.md 导出，置顶句改用上方定位）→ 将【自我介绍】粘贴到求职信框（如有）→ 提交
5. **记录 postId / 申请号**：进入岗位详情后，URL 中 `postId=` 或 `pb/` 后的字符串即 jobId，截图保存。
6. 内推备选：脉脉搜"地平线 内推"找员工，发岗位标题+简历，请其生成内推链接（绕 HR 初筛）。

> ⚠️ 本岗是本轮第一优先：JD 罕见地直接点名"内核/OS经验者优先 + Linux I/O栈优先"，候选人7年内核I/O栈+bpf~1000补丁+百度云IaaS分布式存储是精准对口，务必本周内投。
> ⚠️ hotjob 门户对代理 IP 封禁，必须用本人浏览器；如本人浏览器也偶发拦截，换网络/清 cookie 重试，或走脉脉内推链接（内推链接通常不走 WAF）。
> ⚠️ r44 数据确认同部门 AI基础架构部 在招存储岗（存储&湖仓高级研发 6-30），可作第二选择顺投。
